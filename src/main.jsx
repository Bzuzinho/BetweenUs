import React from 'react'
import ReactDOM from 'react-dom/client'
import BetweenUs from './BetweenUsApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BetweenUs />
  </React.StrictMode>
)
